# macOS / editor noise
.DS_Store
*.swp
*.tmp

# Runtime backups and archives
*.tar
*.tar.gz
*.zip
*.bak
*.backup
*.backup.*
v7-backups/
backups/

# VPN/client secrets and generated client artifacts
*.key
*.pem
*.p12
*.mobileconfig
*.conf
*.ovpn
*.png
*.qr
v7-iphone.conf
v7-iphone.png
clients/
v7-clients/

# Live auth/session material
auth.json
*.session
*.token
*password.txt
*secret.txt
*.secret
*.secrets

# Local logs/state dumps
*.log
*.jsonl
egress-history.jsonl
test-results/

# Retired one-shot repository-wide consumer-audit indexes. Keep the compact
# report, scoped code projection and JSON summary under version control.
docs/reports/engineering/final_consumer_audit_2026-06-30/xref.tsv
docs/reports/engineering/final_consumer_audit_2026-06-30/xref_important.tsv

# Local operational truth/cache, not release evidence
.v7/
