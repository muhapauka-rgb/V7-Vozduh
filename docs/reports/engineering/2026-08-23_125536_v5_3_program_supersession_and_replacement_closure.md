# V5.3 — supersession and safe replacement-closure contract

**Date:** 2026-08-23 12:55 MSK  
**Mission:** `V7_SERVICE_FAILURE_CURRENT_PROGRAM_SUPERSESSION_AND_REPLACEMENT_CLOSURE_CORRECTION`  
**Effect class:** correction of the existing Program contract only.

## Summary

The existing Service Failure Program now preserves one current health target,
`V5.3 N0–N11 ROLE-BASED FAST RECOVERY ARCHITECTURE`, while making eventual
responsibility-scoped removal of safely replaced Runtime mandatory. No old
Program body was removed and no current Runtime behavior was changed.

## Current intake

CPS remains the volatile owner. It reports current stop `NONE`, current frontier
`V7_FAILURE_DETECTION_AND_RECOVERY_LATENCY_OPTIMIZATION` and no Runtime mutation
authority from this document Mission. OMP and CPS consistency passed.

## Corrections consumed

1. The global file status no longer makes every retained historical plan look
   currently executable. It identifies the single current N0–N11 contract and
   leaves live admission to CPS.
2. `CURRENT_ARCHITECTURE_SUPERSESSION_LAW` now states that historical code
   existence does not preserve historical responsibility and that an obsolete
   primary cannot survive by being renamed fallback.
3. The former wording `old roles ... reclassified, not abandoned` was replaced:
   C8 remains only L3 backstop, Full Matrix only L4 deep/disagreement/ambiguous
   fallback, and their former primary meanings are superseded.
4. One current responsibility table records Matrix as the single health/state/
   T0 writer; Planner, registry, governed execution and verification remain
   separate. The mutation boundary refers to existing SYSTEM_MAP/Runtime owners
   and does not invent a generic new Apply owner.
5. All three generic current phrases were reconciled with the existing direct-
   T0 tournament: the product path, `FAST_FALL` and fast-wake precedence now
   distinguish definitive L0 `MODE A / MODE B` handling from mandatory
   confirmation of ambiguous L0/L1P/L1T/L2 evidence.
6. `PER_PHASE_REPLACEMENT_CLOSURE` requires exact caller/consumer/state mapping,
   shared-owner and narrow-lineage checks, same-Matrix Polygon, integrated
   end-to-end proof, controlled Runtime where applicable, consumer migration,
   one bounded fallback window and zero responsibility residue before deletion.
   An earlier phase may remain pending retirement proof until N7/N8 supplies the
   integrated evidence; it cannot delete the safe predecessor early.
7. `EXPLICITLY_DEFERRED` is now bounded by an exact current caller/consumer or
   unfinished integration dependency, owner, role, re-entry, deletion and
   expiry/revalidation conditions. It is illegal after replacement and consumer
   migration.
8. N11 is the final whole-system zero-residue reconciliation, not the first
   cleanup stage. This document makes the future contract binding but does not
   claim current Runtime residue is zero.

## Preserved contracts

- The direct-T0 `MODE A / MODE B` tournament remains unresolved and mandatory.
- Matrix remains the single canonical T0 writer in both modes.
- Ambiguous, stale, unknown, conflicting and correlated evidence remains
  fail-closed; ambiguous evidence always requires independent confirmation.
- Target readiness remains independent and mandatory before governed Apply.
- Client movement remains possible only at the existing governed mutation
  boundary after Planner/Candidate/Packet/Lease/Barrier and Authority gates.
- S11/T11 separation and all current SLO classes are unchanged.
- V4 data-plane semantics and CT-M0F narrow CPS-selectable evidence/validation
  lineage retain their current scope; neither becomes another health system.
- N0a remains the current execution-envelope prerequisite before N8 and any
  production fast-cadence activation.

## Historical disposition and metrics

```text
PROGRAM_LINES_BEFORE = 6586
PROGRAM_LINES_AFTER  = 6715

CURRENT_ACTIVE_EXECUTION_CONTRACTS_BEFORE = 1
CURRENT_ACTIVE_EXECUTION_CONTRACTS_AFTER  = 1

CURRENT_DIRECT_T0_GENERIC_CONTRADICTIONS_BEFORE = 3
CURRENT_DIRECT_T0_GENERIC_CONTRADICTIONS_AFTER  = 0

CURRENT_GLOBAL_STATUS_AMBIGUITY_BEFORE = 1
CURRENT_GLOBAL_STATUS_AMBIGUITY_AFTER  = 0

OLD_ROLES_NOT_ABANDONED_AMBIGUITY_BEFORE = 1
OLD_ROLES_NOT_ABANDONED_AMBIGUITY_AFTER  = 0

DUPLICATE_CURRENT_ARCHITECTURE_IDENTITIES_AFTER = 0
DUPLICATE_MODE_A_MODE_B_CONTRACTS_AFTER = 0
DUPLICATE_N0_N11_ORDERS_AFTER = 0
HISTORICAL_PROGRAM_SECTIONS_REMOVED_WHOLESALE = 0
```

Historical V1–V5 executable-looking headings and old client-latency terms remain
physically present as narrow lineage/history/evidence. The existing current
scope table explicitly prevents them from dispatching or constraining N0–N11.
No line-count reduction was used as a success criterion.

## Validation before publication

- `git diff --check`: PASS.
- Exactly one occurrence exists for the architecture supersession law, current
  responsibility table, direct-T0 tournament, per-phase replacement closure,
  fallback distinction and N0–N11 order.
- CPS atomic live-state consistency: PASS; internal contradiction count `0`.
- Local classification: PASS, documentation-only, no blocking dirtiness.
- Runtime consistency: PASS, `DOCS_ONLY_MISMATCH`, deployment not required.
- The pre-publication overall truth-check remained NO-GO only because its
  sandboxed GitHub reader could not resolve the remote and the change had not
  yet been published.

## Effects

```text
RUNTIME_EFFECT    = NONE
MATRIX_EFFECT     = NONE
TIMER_EFFECT      = NONE
ROUTING_EFFECT    = NONE
CLIENT_EFFECT     = NONE
AUTHORITY_EFFECT  = NONE
CPS_STATE_EFFECT  = NONE
OMP_STATE_EFFECT  = NONE
```

## Publication verification

- Program and this report were committed as
  `377874e4607e162443de1093a451a36c82c3acb0` and pushed to
  `origin/Updatesystem`.
- The authenticated GitHub API independently resolved the remote branch to the
  exact same commit.
- Post-publication CPS, OMP, local and Runtime checks remain PASS; local worktree
  is clean, Runtime is aligned and no deployment is required.
- The truth-check GitHub sub-check still reports its own sandboxed remote reader
  unavailable. This is a checker transport limitation, not an unverified push;
  the independent authenticated API result above supplies remote proof.

## Terminal and next frontier

Document terminal:

`V7_CURRENT_SERVICE_FAILURE_PROGRAM_SUPERSESSION_AND_REPLACEMENT_CLOSURE_CONTRACT_CONSUMED`.

`ZERO_LEGACY_RESIDUE_LAW = BINDING`, but
`ACTUAL_RUNTIME_ZERO_LEGACY_RESIDUE = NOT_CLAIMED_BY_THIS_DOCUMENT_MISSION`.

The exact Program frontier remains N0a. In later admitted N1/N4/N7 work, the
system must classify existing L0 evidence and run the shared `MODE A` versus
`MODE B` Polygon tournament before direct T0 Runtime admission. No predecessor
may be physically removed until its integrated replacement gate passes.
