diff --git a/admin/v7-admin-api b/admin/v7-admin-api
index 14af8ae..328f90c 100755
--- a/admin/v7-admin-api
+++ b/admin/v7-admin-api
@@ -15938,11 +15938,21 @@ def run_json_command(cmd, timeout=40, max_output=5_000_000):
     return {"cmd": cmd, "rc": proc.returncode, "output": redact(output[-12000:]), "json": redact(parsed)}
 
 
-def autoswitch_plan_state(target_egress=""):
-    cmd = ["v7-users-autoswitch", "--pretty"]
+def autoswitch_read_only_plan_command(target_egress=""):
+    cmd = [
+        "v7-users-autoswitch",
+        "--pre-planner-refresh", "write",
+        "--pre-planner-refresh-command", "v7-intelligence-snapshot-refresh",
+        "--pretty",
+    ]
     target_egress = safe_egress_id(target_egress)
     if target_egress:
         cmd.extend(["--target-egress", target_egress])
+    return cmd, target_egress
+
+
+def autoswitch_plan_state(target_egress=""):
+    cmd, target_egress = autoswitch_read_only_plan_command(target_egress)
     result = run_json_command(cmd, timeout=45)
     return {
         "action": "autoswitch_plan",
@@ -15955,10 +15965,7 @@ def autoswitch_plan_state(target_egress=""):
 
 
 def autoswitch_dry_run_state(actor, target_egress=""):
-    cmd = ["v7-users-autoswitch", "--pretty"]
-    target_egress = safe_egress_id(target_egress)
-    if target_egress:
-        cmd.extend(["--target-egress", target_egress])
+    cmd, target_egress = autoswitch_read_only_plan_command(target_egress)
     result = run_json_command(cmd, timeout=45)
     audit_admin(actor, "admin_action_autoswitch_dry_run", f"autoswitch dry-run target={target_egress or 'all'}", object_type="autoswitch", object_id=target_egress or "plan", result="OK" if result.get("rc") == 0 else "FAIL")
     return {"action": "autoswitch_dry_run", "target_egress": target_egress, "rc": result.get("rc"), "plan": result.get("json"), "output": result.get("output", ""), "cmd": result.get("cmd", [])}
diff --git a/tests/unit/test_api3_read_only_views.py b/tests/unit/test_api3_read_only_views.py
index d0fccd6..f742c81 100644
--- a/tests/unit/test_api3_read_only_views.py
+++ b/tests/unit/test_api3_read_only_views.py
@@ -122,6 +122,39 @@ class Api3ReadOnlyViewsTest(unittest.TestCase):
             for token in forbidden:
                 self.assertNotIn(token, source)
 
+    def test_autoswitch_read_only_plan_uses_pre_planner_refresh(self):
+        cmd, target = self.admin.autoswitch_read_only_plan_command("awg0")
+        self.assertEqual(target, "awg0")
+        self.assertEqual(cmd[:5], [
+            "v7-users-autoswitch",
+            "--pre-planner-refresh",
+            "write",
+            "--pre-planner-refresh-command",
+            "v7-intelligence-snapshot-refresh",
+        ])
+        self.assertIn("--pretty", cmd)
+        self.assertEqual(cmd[-2:], ["--target-egress", "awg0"])
+
+    def test_autoswitch_plan_state_reuses_refresh_command(self):
+        calls = []
+        original = self.admin.run_json_command
+        try:
+            def fake_run_json_command(cmd, timeout=45, max_output=12000):
+                calls.append((cmd, timeout, max_output))
+                return {"cmd": cmd, "rc": 0, "json": {"ok": True}, "output": "{}"}
+
+            self.admin.run_json_command = fake_run_json_command
+            result = self.admin.autoswitch_plan_state("vless")
+        finally:
+            self.admin.run_json_command = original
+
+        self.assertEqual(result["rc"], 0)
+        self.assertEqual(result["plan"], {"ok": True})
+        self.assertEqual(result["cmd"], calls[0][0])
+        self.assertIn("--pre-planner-refresh", calls[0][0])
+        self.assertIn("write", calls[0][0])
+        self.assertEqual(calls[0][1], 45)
+
 
 if __name__ == "__main__":
     unittest.main()
