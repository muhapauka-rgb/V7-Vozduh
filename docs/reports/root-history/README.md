# Historical root reports

This directory contains historical report documents moved out of the repository
root. They are not current Runtime truth. Current Programs, CPS/OMP state and
canonical engineering closure reports live in `docs/programs/`,
`docs/reference/` and `docs/reports/engineering/`.
